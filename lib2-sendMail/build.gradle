apply plugin: 'java'
apply plugin: 'maven-publish'

group = 'dam.tscalise_jferrando'
version = '2.5'

task sourceJar(type: Jar) {
    from sourceSets.main.allJava
}

repositories {
    mavenCentral()
    maven {
        url myMavenRepoReadUrl



        credentials {
            username myMavenRepoReadUsername
            password myMavenRepoReadPassword
        }
    }
}

publishing {
    repositories {
        maven {
            url myMavenRepoWriteUrl

            credentials {
                username myMavenRepoWriteUsername
                password myMavenRepoWritePassword
            }
        }
    }

    publications {
        maven(MavenPublication) {
            from components.java

            artifact sourceJar {
                classifier "sources"
            }
        }
    }
}


dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    // https://mvnrepository.com/artifact/com.sun.mail/javax.mail
    compile group: 'com.sun.mail', name: 'javax.mail', version: '1.6.2'
    compile 'dam.tscalise_jferrando:M3_UF2_lib3-FileIO:2.2'

}